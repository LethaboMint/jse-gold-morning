{
  "rules": "dashboard",
  "min_pred": 0.0,
  "skip_duplicate_same_day": true,
  "write_latest_snapshot": true,
  "forward_horizon_days": 22,
  "log_history_trading_days": 44,
  "log_exclude_miners": ["ANG"],
  "comment": "forward_horizon_days = hold length modeled and audited. log_history_trading_days should be ~2x forward_horizon so ~1 month of completed holds appear on the dashboard."
}
